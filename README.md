# AI-First CRM – HCP Log Interaction Module  
Complete backend + frontend + LangGraph + Groq project.

## Run Backend:
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --port 8000

## Run Frontend:
cd frontend
npm install
npm start
